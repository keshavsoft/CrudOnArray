💡 ReadSchema :

📌 **1.AsIs** = Returns the complete schema exactly as defined in the schema JSON file, without any changes or filtering.

📌 **2.Both** = Returns only the field and title from the schema.

📌 **3.FieldsOnly** = Returns only the field values from the schema.


📌 **4.TitlesOnly** = Returns only the title values from the schema.
